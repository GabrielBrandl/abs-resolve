server {
    listen 80;
    server_name absresolve.com.br;

    # Redirecionar para HTTPS (descomente após configurar certificado)
    # return 301 https://$host$request_uri;

    location / {
        root /var/www/abs-resolve/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        rewrite ^/api/(.*) /$1 break;
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    location /uploads/ {
        proxy_pass http://127.0.0.1:3001/uploads/;
    }
}

# server {
#     listen 443 ssl http2;
#     server_name absresolve.com.br;
#
#     ssl_certificate /etc/letsencrypt/live/absresolve.com.br/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/absresolve.com.br/privkey.pem;
#
#     # ... mesmas location blocks acima
# }
